#include"timer.h"

Timer::Timer()
{
}

void Timer::timer_handler()
{
    //每间隔固定时间片就将正在 运行 的进程变为就绪态,可能要调用 进程控制的 运行->就绪

    while(1)
    {
        //将 运行 的进程 变为 就绪

        //调度就绪的进程
        //从就绪队列取出一个进程执行

        //执行结束
    }


}

